PORT=4000
HOST=127.0.0.1

# Optional advisory only. Leave blank to keep the local deterministic advisory
# disabled. Never commit the real key to this file or to Git.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6
